## R CMD check results

0 errors | 0 warnings | 2 notes

* This is a new submission.
* One NOTE about pandoc is from the local Windows environment only
  and does not occur on CRAN's infrastructure.

## Test environments

* local Windows 11, R 4.5.2
* win-builder (devel and release)
* macOS (via mac.r-project.org builder)
* R-hub (linux, macos, windows, ubuntu-clang)

## Downstream dependencies

This is a new package with no reverse dependencies.
