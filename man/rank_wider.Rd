% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_longer.R
\name{rank_wider}
\alias{rank_wider}
\title{Turn Long Ranking Data into a Wide Format}
\usage{
rank_wider(
  x,
  id,
  item = "item_name",
  rank = "ranking",
  output = c("multiple", "single"),
  reference = NULL,
  ranking_name = "ranking"
)
}
\arguments{
\item{x}{A data frame in long format with respondent identifiers, item names,
and ranks.}

\item{id}{The column that uniquely identifies the respondent.}

\item{item}{The column that contains item names. Defaults to
\code{"item_name"}.}

\item{rank}{The column that contains rank values. Defaults to
\code{"ranking"}.}

\item{output}{The desired output format: \code{"multiple"} for one column
per item or \code{"single"} for one pasted ranking string. Defaults to
\code{"multiple"}.}

\item{reference}{Optional character vector giving the reference choice-set
order. If omitted and \code{reference_no} is present in \code{x}, item order
is inferred from that column.}

\item{ranking_name}{The name of the output column when
\code{output = "single"}. Defaults to \code{"ranking"}.}
}
\value{
A data frame in wide format with one row per respondent.
}
\description{
This function takes ranking data in long format and returns a wide-format
data frame with one row per respondent. It can return either one column per
ranked item or a single pasted ranking string.
}
\examples{
x <- data.frame(
  id = c(1, 1, 1, 2, 2, 2),
  item_name = c("A", "B", "C", "A", "B", "C"),
  ranking = c(1, 2, 3, 3, 2, 1)
)

rank_wider(x, id = "id")
rank_wider(
  x,
  id = "id",
  output = "single",
  reference = c("A", "B", "C")
)

}
